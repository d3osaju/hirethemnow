{
  "name": "hirethemnow-api-tests",
  "version": "1.0.0",
  "description": "API tests for HireThemNow endpoints",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "mocha tests/**/*.test.js --timeout 10000",
    "test:auth": "mocha tests/auth.test.js --timeout 10000",
    "test:health": "mocha tests/health.test.js --timeout 10000",
    "test:resume": "mocha tests/resume.test.js --timeout 10000",
    "test:mailbox": "mocha tests/mailbox.test.js --timeout 10000",
    "test:jobs": "mocha tests/jobs.test.js --timeout 10000"
  },
  "dependencies": {
    "chai": "^4.3.10",
    "mocha": "^10.2.0",
    "axios": "^1.6.2",
    "form-data": "^4.0.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/chai": "^4.3.11",
    "@types/mocha": "^10.0.6"
  }
}